{
  "name": "BF-client",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "build": "npm --prefix client-app run build"
  },

  "overrides": {
    "esbuild": "0.18.20",
    "serialize-javascript": "6.0.1"
  }
}

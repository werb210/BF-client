{
  "name": "BF-client",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc -p tsconfig.ci.json --noEmit",
    "test": "vitest run",
    "build": "npm --prefix client-app run build",
    "test:e2e": "echo 'run locally only'",
    "client:build": "npm --prefix client-app run build",
    "client:test": "npm --prefix client-app run test",
    "client:typecheck": "npm --prefix client-app run typecheck"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.0",
    "@types/node": "^20.0.0",
    "@vitejs/plugin-react": "^4.0.3",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}

{
  "name": "BF-client",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "typecheck": "npm --prefix client-app run typecheck",
    "test": "echo 'tests disabled (npm blocked)' && exit 0",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "typescript": "file:typescript-5.9.3.tgz"
  }
}

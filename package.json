{
  "name": "bf-client-root",
  "private": true,
  "workspaces": [
    "client-app"
  ],
  "overrides": {
    "esbuild": "0.18.20",
    "serialize-javascript": "6.0.1"
  },
  "packageManager": "npm@10",
  "engines": {
    "node": ">=20.10 <23"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "preinstall": "npm config delete @eslint:registry || true",
    "build": "npm run verify:no-raw-fetch && npm --prefix client-app run build",
    "test": "npm run verify:no-raw-fetch && npm --prefix client-app run test",
    "dev": "npm --prefix client-app run dev",
    "preview:ci": "npm --prefix client-app run preview:ci",
    "verify:no-raw-fetch": "rg -n \"fetch\\(\" src client-app/src | rg -v \"src/lib/api(Client)?\\.ts|client-app/src/lib/api(Client)?\\.ts\" && exit 1 || exit 0",
    "verify:no-alt-api": "rg -n \"fetch\\(|axios\\(\" src | rg -v \"api.ts\" && exit 1 || exit 0"
  }
}
